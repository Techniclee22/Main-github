# Web experiment (frozen)

The product is the Mac floating pill in `desktop/`.

This Next.js app is an earlier paste-and-share reader. Leave it alone unless someone explicitly asks to revive the web path.

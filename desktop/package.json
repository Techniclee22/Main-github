{
  "name": "read-to-me-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Floating desktop pill that reads whatever window you pick — PDFs, apps, screens.",
  "main": "main.cjs",
  "scripts": {
    "check-api": "node scripts/check-api-contract.cjs",
    "prestart": "npm run check-api",
    "start": "electron .",
    "dev": "npm run check-api && electron ."
  },
  "dependencies": {
    "electron": "^37.2.0",
    "tesseract.js": "^7.0.0"
  }
}
